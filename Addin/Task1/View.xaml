<Window x:Class="Addin.Task1.View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Addin.Task1"
        mc:Ignorable="d"
        Title="Task 1"
        Height="200"
        Width="450"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Number of sheets:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NumberOfSheets}"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Sheet name:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SheetName}"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Sheet number prefix:"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SheetNumberPrefix}"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Sheet number separator:"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SheetNumberSeparator}"/>

        <Button Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Content="Create" Click="Button_Click" Margin="10"/>

    </Grid>
    
</Window>