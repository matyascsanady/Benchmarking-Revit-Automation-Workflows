<Window x:Class="Addin.Task2.View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Addin.Task2"
        mc:Ignorable="d"
        Title="Task 2"
        Height="280"
        Width="450"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Number of Grids X:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NumberOfGridsX}"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Number of Grids Y:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NumberOfGridsY}"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Spacing X [cm]:"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SpacingX}"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Spacing Y [cm]:"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SpacingY}"/>

        <Label Grid.Row="4" Grid.Column="0" Content="Column type:"/>
        <ComboBox Grid.Row="4" Grid.Column="1" ItemsSource="{Binding Types}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedType}"/>

        <Label Grid.Row="5" Grid.Column="0" Content="Level:"/>
        <ComboBox Grid.Row="5" Grid.Column="1" ItemsSource="{Binding Levels}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedLevel}"/>

        <Label Grid.Row="6" Grid.Column="0" Content="Height offset [cm]:"/>
        <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding HeightOffset}"/>

        <Button Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" Content="Generate" Click="Button_Click" Margin="10"/>

    </Grid>
    
</Window>